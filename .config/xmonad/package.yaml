name: myXmonad

dependencies:
    - base
    - xmonad
    - xmonad-contrib
    - split
    - fitspec

executables:
    xmonad:
        source-dirs: src
        main:        xmonad.hs
        ghc-options:
            - -threaded
            - -rtsopts
            - -with-rtsopts=-N
            - -Wall
